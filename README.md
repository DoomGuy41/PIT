PIT (Post‑Install Toolbox) is a lightweight native Win32 utility for IT technicians that simplifies Windows post‑deployment tasks.
It runs elevated (administrator) while correctly targeting the interactive logged‑in user.
